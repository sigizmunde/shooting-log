import styled from 'styled-components';

export const List = styled.div`
  display: flex;
  flex-direction: column;
  align-items: stretch;
  gap: var(--min-gap);
  padding: var(--min-gap);
  width: 100%;
  overflow-y: auto;
`;
